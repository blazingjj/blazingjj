name: Dev preview

on:
  push:
    branches:
      - dev
  workflow_dispatch:

permissions: read-all

jobs:
  preview-release:
    permissions:
      contents: write
    uses: ./.github/workflows/preview-release.yml
    with:
      tag: dev-preview
      title: Dev preview
